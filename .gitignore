# Node.js dependencies
node_modules/
# Ignore the "node_modules" folder because it contains dependencies installed by npm. 
# These can be reinstalled using "npm install" and do not need to be in the repository.

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
# Ignore log files generated by Node.js or npm, as they are only useful for local debugging.

# Environment files
.env
# Ignore sensitive environment configuration files like ".env" 
# that may contain API keys or other confidential settings.

# Build files
dist/
build/
# Ignore build folders (if you compile your client or server in the future).

# System files
.DS_Store
Thumbs.db
# Ignore system-specific files such as .DS_Store (macOS) or Thumbs.db (Windows).

# IDE specific files
.vscode/
.idea/
*.sublime-workspace
*.sublime-project
# Ignore IDE-specific configuration files, such as those for Visual Studio Code, IntelliJ IDEA, or Sublime Text.

# Other
*.lock
*.bak
# Ignore backup files and lock files (e.g., package-lock.json) if you choose not to include them.
